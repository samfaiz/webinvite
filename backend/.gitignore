
# env & local db
.env
*.db
*.db-journal
prisma/dev.db*

# RSVP excel exports (dev mode) — anchored so it won't match src/*/exports
/exports/

# runtime upload storage — anchored so it won't match src/uploads (the module)
/uploads/
