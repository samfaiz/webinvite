# Ornaments

Place decorative art here (served at `/ornaments/...`).

- **laurel-frame.png** — the gold oval laurel frame used behind the monogram
  crest (`MonogramCrest`). Transparent PNG; the initials render centered inside
  the oval. If this file is missing, the crest falls back to a simple drawn laurel.
